# Opossum Search Port Endpoints

Here's a quick reference guide to all the port endpoints in your application:

```
PORT 5000   - Main Flask Application (GraphQL API)
PORT 11434  - Ollama API Service
PORT 6379   - Redis Cache Server
PORT 4318   - OpenTelemetry Collector
```

## Endpoint URLs

### Flask Application
- Main GraphQL: `http://localhost:5000/graphql`
- GraphQL Voyager: `http://localhost:5000/voyager`

### Ollama API
- Base URL: `http://localhost:11434`
- Generate: `http://localhost:11434/api/generate` 
- Chat: `http://localhost:11434/api/chat`
- Models List: `http://localhost:11434/api/tags`
- Running Models: `http://localhost:11434/api/ps`

### Monitoring
- OpenTelemetry: `http://localhost:4318/v1/traces`

### Redis
- `redis://localhost:6379/0`

Keep this reference handy as you continue developing your application. These are the core services you'll need to ensure are running for full functionality.